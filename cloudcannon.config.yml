# CloudCannon Configuration - Minimal & Reliable

collections_config:
  services:
    path: src/services
    name: Services

_inputs:
  title:
    comment: The page title
  category:
    type: select
    options:
      values:
        - Cosmetic
        - Restorative  
        - Family
        - General

generator:
  metadata:
    eleventy: true 